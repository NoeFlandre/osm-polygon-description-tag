# Public Dataset Card Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish a short, deterministic Hugging Face dataset card that explains both dataset configurations, retains the key plots and images, and reports the important base and language statistics without repetition.

**Architecture:** Keep the base card in `docs/dataset-card-template.md`. Keep all base counts and plots generated by `dataset/docs.py`; keep the additive `language-v1` configuration and language statistics generated by `publication/language.py` and installed by `publication/language_card.py`. Extend language export statistics only with deterministic unsupported-language counts needed by the public card.

**Tech Stack:** Markdown/YAML, Python, PyArrow, pytest, Ruff, strict MkDocs, Hugging Face Hub publication verifier.

### Status — 2026-09-23

- **Tasks 1–3 shipped** in `ec8e6a8` (*docs(card): shorten the public card and
  report splitter coverage*), with follow-ups `04ae974`, `89d1866` and `8e4c789`.
  These commits live on `feat/ungate-language-confidence`; they are **not yet
  merged** into `main` or `chore/code-quality-uplift`, where
  `publication/language.py` still has no top-unsupported-language ranking.
- **Task 4 is open.** No regenerated base card, dry-run plan, publication or
  remote verification is recorded for this work. It must also wait for the
  `language-v1` recompute that `d63d82b` (confidence ungating, same branch)
  requires: the cascade fingerprint changed, so the published `language-v1`
  export no longer matches the current configuration.

<details><summary>Paused checkpoint — 2026-09-22 (superseded)</summary>


- Tasks 1–3 are implemented and their focused contract suite passes: 287 passed.
- Ruff format/check, `ty check`, and strict MkDocs pass; MkDocs emits only the upstream Material warning.
- The full suite was intentionally interrupted while its mutation-gate contract was scanning `mutants/`: 208 passed, 1 skipped in 1:27:30; this is not a complete full-suite verdict.
- No base-card regeneration, Grid'5000 submission, Hugging Face upload, or remote mutation was performed in this turn.
- Preserve the pre-existing `M scripts/run_language_grid.py` edit; all other listed changes are the card-release work.
- Resume at Task 4: run the release dry-run, regenerate/validate the local card and language-v1 section, publish, independently verify both Hub configurations, then rerun the full quality gate with an efficient mutation-test strategy.

</details>

---

### Task 1: Lock the compact language statistics contract

**Files:**
- Modify: `tests/unit/publication/test_language_stats_contracts.py`
- Modify: `tests/unit/publication/test_split_stats_contracts.py`
- Modify: `tests/unit/publication/test_language_card.py`
- Modify: `tests/unit/publication/test_language_hub.py`

- [x] Add failing assertions for deterministic top unsupported-language ranking, split coverage, and the compact public wording.
- [x] Run the focused publication tests and confirm they fail because the new statistics and wording do not exist.

### Task 2: Implement deterministic language-card statistics

**Files:**
- Modify: `src/osm_polygon_description_tag/publication/language.py`

- [x] Accumulate `unsupported_language_<iso6393>` reasons without retaining annotation rows.
- [x] Serialize and read the ranked top unsupported languages with backward-compatible empty defaults.
- [x] Render split eligibility, supported/unsupported counts, percentages, top unsupported languages, and concise detector provenance with no removed score/margin-gate claim.
- [x] Run the focused tests and keep the implementation streaming and deterministic.

### Task 3: Replace the verbose base card template and generated block

**Files:**
- Modify: `docs/dataset-card-template.md`
- Modify: `src/osm_polygon_description_tag/dataset/docs.py`
- Modify: `tests/unit/dataset/test_docs_helpers.py`

- [x] Reduce the public prose to purpose, contents, compact generated metrics, schema, method, limitations, license, and citation.
- [x] Keep the hero image, H3 density map, and area-distribution plot with one-line captions.
- [x] Remove repeated geometry/count sections while retaining their key values in the generated summary.
- [x] Add regression assertions for deterministic section ordering and no hand-written counts.

### Task 4: Generate, validate, and publish

**Files:**
- Generated only: `data-root/README.md`, `data-root/stats.json`, `data-root/assets/*`

- [ ] Regenerate the base card from validated Parquet artifacts and the language card from the published `language-v1` export.
- [ ] Run focused tests, full pytest, Ruff, type checks, and strict MkDocs.
- [ ] Run the exact dry-run publication plan and verify the allowlisted metadata/artifact hashes.
- [ ] Publish only after all local gates pass, then independently verify the remote card, both configs, plots, and stats through Hub metadata and Dataset Viewer APIs.
